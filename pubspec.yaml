name: food_label_ai
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_generative_ai: ^0.4.7
  image_picker: ^1.2.1
  permission_handler: ^12.0.1
  http: ^1.6.0
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# This is the ONLY flutter: section you need
flutter:
  uses-material-design: true

  # Your .env file is now correctly listed as an asset
  assets:
    - .env